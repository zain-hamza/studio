{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile information.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        }
      },
      "required": ["displayName", "email"]
    },
    "SavedRole": {
      "title": "Saved Role",
      "description": "A career role that a user has saved for future reference.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the career role."
        },
        "responsibilities": {
          "type": "string",
          "description": "The key responsibilities of the role."
        },
        "skills": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of required skills for the role."
        },
        "tools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "A list of typical tools used in the role."
        },
        "parentSubfield": {
          "type": "string",
          "description": "The name of the subfield this role belongs to."
        },
        "savedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the role was saved."
        }
      },
      "required": ["name", "parentSubfield", "savedAt"]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public profile information for each user."
    },
    "/users/{userId}/savedRoles/{roleId}": {
      "schema": {
        "$ref": "#/entities/SavedRole"
      },
      "description": "A subcollection storing the career roles saved by a user."
    }
  }
}
